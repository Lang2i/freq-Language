# freq 编程语言 - 功能特性

freq 是一门高性能、跨平台的编程语言，专为**纯 Vibe 编程**设计——让编写代码变得自然流畅，执行速度飞快。

---

## 目录

1. [语言优势](#语言优势)
2. [核心特性](#核心特性)
3. [编译后端对比](#编译后端对比)
4. [当前局限](#当前局限)
5. [V0.2 路线图](#v02-路线图)
6. [更新日志](#更新日志)

---

## 语言优势

### 1. **纯 Vibe 编程体验**
- **零仪式感**：代码读起来就像你的思考过程
- **即时反馈**：JIT 模式支持快速开发循环
- **原生性能**：编译为优化的机器码
- **安全优先**：内置内存安全检查
- **嵌入式设计**：极小运行时，完美适配嵌入式系统

### 2. **类 C 语法，易于学习**
- 熟悉的 C 风格语法，降低学习门槛
- 支持 `if-elif-else`、`switch`、`while`、`for`、`fo` 等标准控制结构
- 直观的语法设计，代码可读性高

### 3. **完善的类型系统**
- 整数类型：`int`（32位）、`int64`（64位）
- 浮点类型：`float`、`double`
- 字符类型：`char`
- 布尔类型：`bool`（true/false）
- 字符串类型：`string`

### 4. **灵活的函数系统**
- 支持两种参数语法风格（C 风格和 Go 风格）
- 返回类型在 `return` 语句中指定，支持动态返回类型
- 程序入口为 `main()` 函数

### 5. **强大的数组支持**
- 多维数组：支持一维、二维、三维及更高维度
- 数组长度属性：`arr.len` 获取数组长度

### 6. **完善的循环控制**
- `break` / `continue`：标准循环控制
- `fo` 循环：范围遍历语法，支持 `fo (start .. end) [var] { ... }`
- `quit` / `exit`：退出循环并继续执行后续代码

### 7. **智能变量声明**
- `var`：普通可变变量
- `let`：一次性常量（仅声明时可赋值）
- `const`：编译时常量

### 8. **实用的数据结构**
- **结构体**：支持自定义数据结构
- **枚举**：支持自动递增值和手动赋值
- **指针**：支持地址操作和解引用

### 9. **内存安全检查**
- 未初始化变量使用检测
- 空指针解引用检测
- 数组索引越界检测

### 10. **模块化系统**
- `mod`：模块声明
- `import`：导入整个模块，可以访问所有内容
- `use`：导入模块特定成员（函数无需 `out`，变量需要）
- `out`：标记变量为可导出状态
- `::`：模块访问操作符

### 11. **开发友好特性**
- 自动输出程序执行时间（毫秒级精度），包含解析、编译、运行各阶段耗时
- 先显示程序运行结果，再显示时间统计信息
- 有警告时不显示运行时间
- 清晰的错误信息，包含行号定位
- 支持转义字符：`\n`、`\t`、`\r` 等

### 12. **两种运行模式**
- **编译模式**：生成可执行文件，适合生产环境
  ```bash
  ./freq test/example.fq -o output  # 编译
  ./output                           # 运行
  ```
- **JIT 模式**：直接运行，适合开发调试
  ```bash
  ./freq -r test/example.fq  # 直接运行，不生成可执行文件
  ```

---

## 核心特性

### V0.1 已实现功能

| 类别 | 特性 | 状态 |
|------|------|------|
| 变量声明 | `var`, `let`, `const` | ✅ |
| 数据类型 | `int`, `int64`, `float`, `double`, `bool`, `string`, `char` | ✅ |
| 控制流 | `if-elif-else`, `switch`, `while`, `for`, `fo` | ✅ |
| 循环控制 | `break`, `continue`, `quit`, `exit` | ✅ |
| 函数 | C 风格/Go 风格参数，动态返回类型 | ✅ |
| 数组 | 多维数组，`.len` 属性 | ✅ |
| 结构体 | 自定义数据结构 | ✅ |
| 枚举 | 自动/手动赋值 | ✅ |
| 指针 | 地址操作，解引用 | ✅ |
| 类型转换 | C 风格和方法风格 | ✅ |
| 模块化 | `mod`, `import`, `use`, `out` | ✅ |
| 编译 | LLVM IR 生成，JIT 模式 | ✅ |
| 内存安全 | 未初始化检测，空指针检测 | ✅ |

---

## 编译后端对比

### 旧版：C 代码生成

**编译流程：**
```
freq源码 → AST → C代码 → GCC → 可执行文件
```

**特点：**
- 生成的 C 代码可读性较好
- 依赖 GCC 编译器
- 编译速度较慢（需要两次编译）
- 优化依赖 GCC 的优化级别

---

### 新版：LLVM IR 代码生成

**编译流程：**
```
freq源码 → AST → LLVM IR → Clang → 可执行文件
```

**特点：**
- 直接生成 LLVM IR（中间表示）
- 利用 LLVM 的优化管道
- 支持 JIT 即时运行模式
- 支持更多优化选项

**性能对比（实测数据）：**

| 方面 | C 代码生成（旧版） | LLVM IR 生成（新版） |
|------|-------------------|---------------------|
| 编译时间（简单程序） | ~150-200 ms | **~50-70 ms**（快 2-3 倍） |
| JIT 首次运行 | 不支持 | ~30-40 ms |
| JIT 后续运行（缓存） | 不支持 | **~2-5 ms** |
| 编译后运行速度（100万次循环） | ~0.5-1 ms | **~0.37 ms**（接近原生 C） |
| 代码质量 | 依赖手写模板 | LLVM 优化器自动优化 |
| 优化能力 | 有限（手写优化） | 强大（LLVM Pass） |

**实测性能数据：**

通过 `test/perf.fq` 测试（100万次循环求和）：
- **编译模式**：编译时间 ~74ms，运行时间 ~0.37ms
- **JIT 模式**：首次运行 ~43ms，后续运行 ~40ms（纯执行时间）

---

## 当前局限

### 1. **标准库有限**
- 当前仅实现基础功能（打印、数学运算）
- 缺乏图形、网络、文件系统等高级库

### 2. **调试工具有限**
- 无内置调试器
- 错误信息虽然清晰，但缺少堆栈跟踪

### 3. **平台支持有限**
- 当前仅支持 macOS/Linux 开发环境
- Windows 支持需要额外配置
- ARM 目标平台尚未完全支持

### 4. **缺乏并发支持**
- 无多线程支持
- 无协程/异步编程

---

## V0.2 路线图

### 计划功能

1. **标准库扩展**
   - 文件 I/O 操作
   - 基础网络支持
   - 数学库
   - 字符串工具

2. **并发支持**
   - 基础线程
   - 原子操作
   - 线程安全数据结构

3. **调试工具**
   - 基础调试器集成
   - 堆栈跟踪
   - 断点支持

4. **平台改进**
   - 更好的 Windows 支持
   - ARM64 优化
   - 初步 WASM 目标

5. **语言增强**
   - Lambda 函数
   - 泛型
   - 错误处理

### 目标时间表
- Q2 2026: 标准库
- Q3 2026: 并发支持
- Q4 2026: 调试器 & 平台扩展

---

## 更新日志

### 2026-05-19
- **重大升级**：将编译后端从 C 代码生成切换为 LLVM IR 生成
- **性能优化**：添加 LLVM 初始化缓存，JIT 后续运行速度提升约 10 倍
- **添加 int64 类型**：支持 64 位整数，解决整数溢出问题
- **修复循环生成**：修复 for 循环的 LLVM IR 生成问题
- **编译速度提升**：从 ~150ms 降至 ~50-70ms（快 2-3 倍）
- **运行速度提升**：编译后的代码运行速度接近原生 C（100万次循环约 0.37ms）
- **添加 JIT 模式**：支持即时运行，适合开发调试
- **新增 `fo` 循环语法**：支持范围遍历 `fo (start .. end) [var] { ... }`
- **新增 `quit` / `exit` 循环控制**：退出循环并继续执行后续代码
- **完善循环控制**：`break` 和 `continue` 语句现在可以正确工作
- **完善模块系统**：函数无需 `out` 关键字即可被 `use` 导入
- **警告优化**：有警告时不显示运行时间

### 2026-05-15
- 优化编译配置（-O2），提升运行速度约 45%
- 完善模块化系统，支持 `import`/`use` 语法
- 添加 `::` 模块访问操作符
- **新增二进制编译功能**：支持生成独立可执行文件
- 使用方式：`freq input.fq -o output`
- 修复 `print_value_raw` 函数中未处理的 `VAL_ARRAY` 和 `VAL_STRUCT` 类型警告
- 更新语言优势和改进计划文档

### 2026-05-14
- 修复 `printn()` 函数无法正常工作的问题
- 完善结构体字面量解析
- 新增内存安全检查：未初始化变量、空指针检测
- 新增枚举（Enum）支持
- 新增全局/局部变量作用域规则

### 2026-05-13
- 优化哈希表查找算法，提升变量和函数查找性能
- 添加变量缓存机制，加速标识符解析
- 修复数组索引越界检测

### 2026-05-12
- 添加 `let` 关键字，支持一次性常量声明
- 完善 `fo` 循环语法，支持数组遍历和范围遍历
- 添加数组 `len` 属性

### 2026-05-11
- 添加结构体（Struct）支持
- 添加指针类型和指针操作
- 添加类型转换功能

---

*freq Programming Language - Pure Vibe Coding for the Modern Developer*

**Version:** 0.1.0  
**Status:** Active Development